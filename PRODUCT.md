# Product

## Register

product

## Users

主要用户是课程视频制作者本人，在桌面大屏上连续审查 Git Course 的分集、Scene、Candidate、Current、Release 与审查证据。用户熟悉生产流程，关心的是当前真实产物、待处理变化和下一道门禁，而不是面向新手的流程讲解。

## Product Purpose

把分散在 orchestrator、manifest、verdict 和视频文件中的生产状态收敛为一个固定视口工作台。成功意味着用户无需整页滚动或切换多个目录，就能选择分集与具体片段、对照准确版本、识别未构建源码变化，并只通过已实现的 orchestrator 动作推进生产阶段。

## Brand Personality

安静、精确、克制。界面应像可靠的专业制作工具：信息密度高但层级明确，文案短而具体，状态表达可信，不用装饰制造“高级感”。

## Anti-references

不做需要整页上下滚动的长页面，不做为手机和平板妥协的大量响应式分支，不做令牌或只读模式等与当前局域网使用无关的外壳。不使用高饱和渐变、发光科技风、装饰动效、重复状态文案，也不把 preview、目录存在或旧文件伪装成 Candidate、Current、Release 或 Published。

## Design Principles

- 真实产物优先：版本、片段和状态必须来自 adapter/orchestrator 已登记的证据。
- 点击完成审查：分集、阶段、版本和 Scene 在固定视口内原位切换。
- 发布与改动分层：已发布是稳定事实，源码变化是后续修订提示，二者不能互相覆盖。
- 主次明确：画面审查始终占据中心，状态与操作只提供决策所需信息。
- 少而准确：删除重复标签、解释性废话和没有实际动作的控件。

## Accessibility & Inclusion

面向键盘和鼠标的大屏操作，保留清晰焦点、语义化按钮和 J/K Scene 切换。状态不能只依赖颜色，应同时提供符号或短文本；动效只用于状态反馈并保持短促。当前不以小屏适配为目标。
