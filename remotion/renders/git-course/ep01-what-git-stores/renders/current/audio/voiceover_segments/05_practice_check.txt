现在用一个已有仓库验证刚才的模型。<#0.25#>先运行 git log --oneline。

你会看到一串 commit：C2，<#0.2#>C1，<#0.2#>C0。<#0.25#>它们不是文件夹名字，而是历史里的节点。

再打开最近一次提交：git show --stat HEAD。<#0.25#>这里先不用理解所有字段，只看它能告诉你：这次提交改了哪些文件，改动规模大概是多少。

如果只想看路径，<#0.25#>可以用 git show --name-only HEAD。

所以这一段实践只证明一件事：<#0.25#>commit 可以被列出、打开和检查。
