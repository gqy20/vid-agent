Git 还会给内容计算哈希。<#0.35#>内容变了，<#0.2#>哈希就变。

哈希不是装饰性的编号。<#0.35#>它更像内容的指纹。

只要改动一个字符，<#0.25#>新的哈希就会出现。<#0.35#>Git 因此能判断，这个对象已经变了。

所以 Git 检查对象时，<#0.25#>不是相信文件名，<#0.25#>而是看内容能不能对上这个身份。

这样 Git 不是靠文件名猜版本，<#0.35#>而是用内容确认，保存的东西没有被悄悄改坏。

后面看到 commit、tree、blob 时，<#0.3#>先记住：<#0.25#>Git 用内容定位对象。
