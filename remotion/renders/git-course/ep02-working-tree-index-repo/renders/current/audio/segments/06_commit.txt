执行 git commit 时，<#0.25#>Git 读取的是 Index，<#0.25#>不是编辑器里最新那份 Working Tree。

你看到 C1 生成，<#0.25#>是因为 Index 里的 v1 被写进 Repository。<#0.35#>

这里要特别留意：<#0.25#>工作区的 v2 还在外面，它不会因为 commit 自动进去。<#0.35#>

所以 C1 保存的是 v1。<#0.25#>commit 之后如果还有未提交修改，通常就是因为工作区已经领先于 Index。<#0.3#>

下一次如果想提交 v2，<#0.25#>还要再 add，再 commit。
