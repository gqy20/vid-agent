关键细节来了：<#0.25#>add 之后你又继续编辑，Working Tree 会变成 v2。

可是 Index 里，<#0.25#>还停在刚才 add 的 v1。

画面里左边和右边不同，<#0.25#>不是 bug，而是在说明同一个 app.js 同时有 staged 和 unstaged 的变化。

第一列说暂存区有变化，<#0.25#>第二列说工作区又有变化。

git status 里看到 MM，<#0.25#>就是这个意思。<#0.25#>要提交 v2，需要重新 add。
