Claude Code 的问题，先看证据。三十天里，失败率、长会话和 Token 同时上升。
cc-insights 直接给 finding：哪个工具、哪个项目、哪个 session 拖慢了你。
rec 先判断，再给下一步动作，不让报表停在 Top N。
tok 和 cmd 下钻到 Token、命令和失败样例。
web 本地打开 Dashboard，成本落到模型、会话和项目。
最后，把诊断交给 AI：JSON、Markdown、Table 都能继续处理。
一条命令开始：cc-insights rec。
