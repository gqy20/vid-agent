# Git Course Manim 历史资产

本目录为旧版本复现和设计追溯保留，不再进入 Git Course 的现行生产流程。

- 不新增 Git Course Manim scene。
- orchestrator 不调用本目录脚本，也不把输出纳入 scene 指纹、candidate 或 release。
- 需要复用其中的教学表达时，将状态与关系重写为 `remotion/src/videos/git-course/kit/` 下的 Remotion 组件，并重新完成 scene 审查。

