# Task Board

A small deterministic service used by the Claude Code Course. The current
lesson follows task creation from the HTTP adapter into the domain service and
the in-memory store.

Run the tests with `pnpm test`.
