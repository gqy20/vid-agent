# Task Board project instructions

- Use pnpm.
- Keep domain validation in `src/tasks` and HTTP adaptation in `src/http`.
- Run `pnpm test`, `pnpm typecheck`, and `pnpm build` before completion.
- Do not edit `generated/` by hand.
