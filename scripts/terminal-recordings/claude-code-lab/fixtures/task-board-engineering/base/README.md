# Task Board

A deterministic course project. Create a task through the HTTP adapter, keep
domain rules in `src/tasks`, and render the result through `src/ui`.

Run `pnpm test`, `pnpm typecheck`, and `pnpm build` before describing a change
as complete.
