# Task Board fixture

Small deterministic project shared by Claude Code Course season one. EP02 starts
with one real failing test in `validate-token.test.js`; the episode explains the
failure without editing the source. `CLAUDE.md` defines the read-only lesson
contract, while project settings deny model-side `Write`, `Edit`, and `Bash`.
The learner's explicit `!pnpm test` still runs through Shell mode.
