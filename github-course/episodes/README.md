# Episode content sources

正式制作的每一集只在本目录维护一个 `<episode-id>.json`，并遵循 `../episode.schema.json`。

不要为单集创建同名脚本目录、独立旁白源或手工维护的 scene 时间线。浏览器 scenario 位于 `scripts/browser-recordings/github-course-lab/scenarios/`，但其 recording id、进入 scene 和教学用途仍由 episode JSON 声明。
